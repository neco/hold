%h2 Orders

%table
  %thead
    %tr
      %th Event
      %th Venue
      %th Section
      %th Row
      %th Quantity
      %th State

  %tbody
    - @orders.each do |order|
      %tr
        %td= order.event
        %td= order.venue
        %td= order.section
        %td= order.row
        %td= order.quantity
        %td= order.state
